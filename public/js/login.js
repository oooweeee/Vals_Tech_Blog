const loginFormHandler = async (event) => {
    event.preventDefault();

// collect values from the login form

// send a post request to the API endpoint

// if it is successful, reirect the browser to the profile page

}